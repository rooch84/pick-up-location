{
  "name": "Pick-up-Box",
  "private": true,
  "scripts": {
    "start": "meteor run"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.36",
    "meteor-node-stubs": "^0.3.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "enzyme": "^3.3.0",
    "enzyme-adapter-react-16": "^1.1.1",
    "phantomjs-prebuilt": "^2.1.16"
  },
  "scripts": {
    "test": "TEST_BROWSER_DRIVER=phantomjs meteor test --once --driver-package meteortesting:mocha",
    "test-as-root": "TEST_BROWSER_DRIVER=phantomjs meteor test --allow-superuser --once --driver-package meteortesting:mocha"
  }
}
