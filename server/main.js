import { Meteor } from 'meteor/meteor';
import '../imports/api/autocomplete.js';

/**
 * Server-side entry point.  We don't need
 * to do anything here other than make
 * sure autocomplete.js is imported
 */
Meteor.startup(() => {
});
